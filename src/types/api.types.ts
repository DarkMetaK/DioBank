export interface IUserData {
    id: string;
    email: string;
    password: string;
    name: string;
    balance: number;
}

export interface IUserDataStorage {
  id: string;
  email: string;
  name: string;
  balance: number;
}